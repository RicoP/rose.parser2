#include "stdio.h"
#include <math.h>

//
// Hello World

void foo();
void baz() {}

int fib(int n) {
  if (n == 0) { return 0; } 
  if (n == 1) { return 1; }
  return fib(n - 1) + fib(n - 2);
}

//
// Hallo Ballo

int main() {
  
  int n;
  int i = 0;
  
  while (i < 10) {
    n = fib(10);
    print(n);
    i = i + 1;
  }

  do {
    print("Hi!");    
  } while(false) ;

  int x = -42;
  float y = 3.14f;
  double z = .001;

  return 0;
}
