#pragma once

// this macro will be ignored my the compiler but serves as a 
// placeholder for meta information for the rose header parser.
#define $(...)

struct RIdentity {};

static inline RIdentity identity;